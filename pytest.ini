[pytest]
# Optional: Define how pytest should interpret imports
# (e.g., add the src directory to the Python path)
pythonpath = src

# Optional: Use an explicit log level for the pytest output
log_level = INFO
